[project]
name = "learntts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "pip>=26.1.2",
    "torch>=2.12.0",
    "torchcodec>=0.14.0",
]

[dependency-groups]
dev = [
    "ipython>=8.39.0",
]
